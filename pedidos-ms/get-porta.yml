info:
  name: get-porta
  type: http
  seq: 2

http:
  method: GET
  url: "{{gateway}}/pedidos-ms/pedidos/porta"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
