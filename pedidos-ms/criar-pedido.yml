info:
  name: criar-pedido
  type: http
  seq: 3

http:
  method: POST
  url: "{{gateway}}/pedidos-ms/pedidos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
