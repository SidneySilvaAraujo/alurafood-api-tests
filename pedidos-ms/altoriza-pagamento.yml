info:
  name: altoriza-pagamento
  type: http
  seq: 4

http:
  method: PUT
  url: "{{gateway}}/pedidos-ms/pedidos/2/pago"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
