info:
  name: criar-pagamento
  type: http
  seq: 2

http:
  method: POST
  url: "{{gateway}}/pagamentos-ms/pagamentos/cadastrar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
